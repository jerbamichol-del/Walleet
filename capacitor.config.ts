import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gestorespese',
  appName: 'Gestore Spese',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
